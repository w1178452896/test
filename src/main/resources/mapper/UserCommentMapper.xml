<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.taylorsfan.blog.repository.UserCommentMapper">
    <resultMap id="BaseResultMap" type="com.taylorsfan.blog.model.relation.UserComment">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="user_id" jdbcType="INTEGER" property="userId"/>
        <result column="comment_id" jdbcType="INTEGER" property="commentId"/>
    </resultMap>
    <sql id="Base_Column_List">
        id, user_id, comment_id
    </sql>
    <insert id="insert" parameterType="com.taylorsfan.blog.model.relation.UserComment" useGeneratedKeys="true"
            keyProperty="id">
        insert into user_comment (id, user_id, comment_id
        )
        values (#{id,jdbcType=INTEGER}, #{userId,jdbcType=INTEGER}, #{commentId,jdbcType=INTEGER}
        )
    </insert>
    <select id="count" parameterType="integer" resultType="integer">
        select count(*) from user_comment WHERE user_id = #{id}
    </select>
    <delete id="deleteByUserId" parameterType="integer">
        DELETE FROM user_comment WHERE user_id = #{userId}
    </delete>
    <delete id="deleteByCommentId" parameterType="integer">
        DELETE FROM user_comment WHERE comment_id = #{commentId}
    </delete>
</mapper>