<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.taylorsfan.blog.repository.relation.BlogCommentMapper">
    <resultMap id="BaseResultMap" type="com.taylorsfan.blog.model.relation.BlogComment">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="blog_id" jdbcType="INTEGER" property="blogId"/>
        <result column="comment_id" jdbcType="INTEGER" property="commentId"/>
    </resultMap>
    <sql id="Base_Column_List">
        id, blog_id, comment_id
    </sql>
    <insert id="insert" parameterType="com.taylorsfan.blog.model.relation.BlogComment" useGeneratedKeys="true"
            keyProperty="id">
        insert into blog_comment (blog_id, comment_id
        )
        values (#{blogId,jdbcType=INTEGER}, #{commentId,jdbcType=INTEGER}
        )
    </insert>
    <delete id="deleteByBlogId" parameterType="java.lang.Integer">
        delete from blog_comment
        where id = #{id,jdbcType=INTEGER}
    </delete>
    <select id="count" parameterType="integer" resultType="integer">
        select count(*) from blog_comment WHERE blog_id = #{id}
    </select>
    <select id="selectAllByBlogId" parameterType="integer" resultMap="BaseResultMap">
        select * from blog_comment WHERE blog_id = #{blogId}
    </select>
</mapper>